services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3030:3000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_API_URL=http://api:8000
    restart: always
    networks:
      - app-network

networks:
  app-network:
    driver: bridge
